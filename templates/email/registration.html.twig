{#<p> <h3>Welcome to the app</h3> </p>#}
{#<p>Hello {{ user.fullName }} you have registered to the app! </p>#}

{#<p>Please confirm account by clicking this : <a href="{{ url('security_confirm',{'token': user.confirmationToken }) }}">Link</a></p>#}

{#<p>To login, go to <a href="{{ url('security_login') }}" > Login Page</a>.</p>#}

{#<p>Thanks!</p>#}

{{ 'email.registration'|trans({
    '%name%': user.fullname,
    '%confirm-link%': url('security_confirm',{'token': user.confirmationToken }),
    '%login-link%':  url('security_login')

})|raw }}